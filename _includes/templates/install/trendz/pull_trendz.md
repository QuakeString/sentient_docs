Run the following commands to verify that you can pull the images from the Docker hub.

```bash
docker pull sentient/sentient-analytics:{{ site.release.sentient-analytics_ver }}
docker pull sentient/sentient-analytics-python-executor:{{ site.release.sentient-analytics_ver }}
```
