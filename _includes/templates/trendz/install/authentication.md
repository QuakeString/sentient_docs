You can access SENTIENT ANALYTICS UI by default at http://localhost:8888

SENTIENT ANALYTICS uses SENTIENT as an authentication service, so for first authentication, 
you need to use **Tenant Administrator** credentials from your **SENTIENT**.

### Topology Discovery

{% include templates/sentient-analytics/install/topology-discovery.md %}
