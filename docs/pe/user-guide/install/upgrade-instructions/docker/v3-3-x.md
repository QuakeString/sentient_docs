---
layout: docwithnav-pe
title: SENTIENT Professional Edition v3.3.x upgrade instructions for Docker
description: SENTIENT Professional Edition v3.3.x upgrade guide for Docker
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/pe/user-guide/install/upgrade-instructions/'
---

* TOC
{:toc}

### Upgrading SENTIENT Professional Edition to 3.3.4.1

{% capture difference %}
**NOTE:**
<br>
These upgrade steps are applicable for SENTIENT version 3.3.4PE. In order to upgrade to 3.3.4.1PE you need to [**upgrade to 3.3.4PE first**](#upgrading-sentient-pe-to-334).
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT Professional Edition image download

Download SENTIENT Professional Edition Docker image:

```bash
docker pull sentient/tb-pe:3.3.4.1PE
```
{: .copy-code}

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT `mytbpe` container if it is running.

```bash
docker compose stop mytbpe
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytbpe**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbpe:
    image: "sentient/tb-pe:3.3.4.1PE"
< . . . >
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT Professional Edition to 3.3.4

{% capture difference %}
**NOTE:**
<br>
These upgrade steps are applicable for SENTIENT version 3.3.3PE. In order to upgrade to 3.3.4PE you need to [**upgrade to 3.3.3PE first**](#upgrading-sentient-pe-to-333).
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT Professional Edition image download

Download SENTIENT Professional Edition Docker image:

```bash
docker pull sentient/tb-pe:3.3.4PE
```
{: .copy-code}

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT `mytbpe` container if it is running.

```bash
docker compose stop mytbpe
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytbpe**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbpe:
    image: "sentient/tb-pe:3.3.4PE"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.3.3' | sudo tee ~/.mytbpe-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker compose run mytbpe upgrade-tb.sh
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose run mytbpe upgrade-tb.sh**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT Professional Edition to 3.3.3

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.2PE. In order to upgrade to 3.3.3PE you need to [**upgrade to 3.3.2PE first**](#upgrading-sentient-pe-to-332).

{% include templates/install/tb-333-update.md %}

#### SENTIENT Professional Edition image download

Download SENTIENT Professional Edition Docker image:

```bash
docker pull sentient/tb-pe:3.3.3PE
```
{: .copy-code}

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT `mytbpe` container if it is running.

```bash
docker compose stop mytbpe
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytbpe**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbpe:
    image: "sentient/tb-pe:3.3.3PE"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.3.2' | sudo tee ~/.mytbpe-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker compose run mytbpe upgrade-tb.sh
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose run mytbpe upgrade-tb.sh**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT Professional Edition to 3.3.2

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.1PE. In order to upgrade to 3.3.2PE you need to [**upgrade to 3.3.1PE first**](#upgrading-sentient-pe-to-331).

#### SENTIENT Professional Edition image download

Download SENTIENT Professional Edition Docker image:

```bash
docker pull sentient/tb-pe:3.3.2PE
```
{: .copy-code}

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT `mytbpe` container if it is running.

```bash
docker compose stop mytbpe
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytbpe**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbpe:
    image: "sentient/tb-pe:3.3.2PE"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.3.1' | sudo tee ~/.mytbpe-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker compose run mytbpe upgrade-tb.sh
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose run mytbpe upgrade-tb.sh**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT Professional Edition to 3.3.1

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.0PE. In order to upgrade to 3.3.1PE you need to [**upgrade to 3.3.0PE first**](#upgrading-sentient-pe-to-33).

#### SENTIENT Professional Edition image download

Download SENTIENT Professional Edition Docker image:

```bash
docker pull sentient/tb-pe:3.3.1PE
```
{: .copy-code}

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT `mytbpe` container if it is running.

```bash
docker compose stop mytbpe
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytbpe**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbpe:
    image: "sentient/tb-pe:3.3.1PE"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.3.0' | sudo tee ~/.mytbpe-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker compose run mytbpe upgrade-tb.sh
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose run mytbpe upgrade-tb.sh**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT Professional Edition to 3.3

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.2.2PE. In order to upgrade to 3.3PE you need to [**upgrade to 3.2.2PE first**](/docs/pe/user-guide/install/upgrade-instructions/docker/v3-2-x/#upgrading-sentient-pe-to-322).

#### SENTIENT Professional Edition image download

Download SENTIENT Professional Edition Docker image:

```bash
docker pull sentient/tb-pe:3.3.0PE
```
{: .copy-code}

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT `mytbpe` container if it is running.

```bash
docker compose stop mytbpe
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytbpe**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbpe:
    image: "sentient/tb-pe:3.3.0PE"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.2.2' | sudo tee ~/.mytbpe-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker compose run mytbpe upgrade-tb.sh
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose run mytbpe upgrade-tb.sh**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}