---
layout: docwithnav-pe
title: SENTIENT Professional Edition v3.2.x upgrade instructions for Ubuntu
description: SENTIENT Professional Edition v3.2.x upgrade guide for Ubuntu
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/pe/user-guide/install/upgrade-instructions/'
---

* TOC
{:toc}

{% include docs/pe/user-guide/install/prepare.md %}

### Upgrading SENTIENT Professional Edition to 3.2.2

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.2.1PE. In order to upgrade to 3.2.2PE you need to [**upgrade to 3.2.1PE first**](#upgrading-sentient-pe-to-321).
[**Prepare**](#prepare-for-upgrading-sentient) for upgrading SENTIENT.

<br>

{% capture tb_3_2_2pe_java_11_linux %}
**NOTE: Since SENTIENT version 3.2.2PE Java 11 is used**
- Please install Java 11 before proceeding upgrade procedure using the following guide:
  - [Java 11 Installation on Ubuntu](/docs/user-guide/install/pe/ubuntu/#step-1-install-java-11-openjdk)
  - [Java 11 Installation on CentOS/RHEL](/docs/user-guide/install/pe/rhel/#step-1-install-java-11-openjdk)

{% endcapture %}
{% include templates/warn-banner.md content=tb_3_2_2pe_java_11_linux %}


#### SENTIENT Professional Edition package download

{% include resources.liquid version="3.2.2" kind="ubuntu-download" pe="true" %}  

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT service if it is running.

```bash
sudo service sentient stop
```
{: .copy-code}

* Install Sentient Web Report component as described [here](/docs/user-guide/install/pe/ubuntu/#step-9-install-sentient-webreport-component).


{% include resources.liquid version="3.2.2" kind="ubuntu-installation" pe="true" %}  

**NOTE:** Package installer will ask you to merge your sentient configuration. It is preferred to use **merge option** to make sure that all your previous parameters will not be overwritten.  
Please make sure that you set **database.ts.type** parameter value (in the file **/etc/sentient/conf/sentient.yml**) to "cassandra" instead of "sql" if you are using Cassandra database for timeseries data:

```
database:
  ts_max_intervals: "${DATABASE_TS_MAX_INTERVALS:700}" # Max number of DB queries generated by single API call to fetch telemetry records
  ts:
    type: "${DATABASE_TS_TYPE:sql}" # cassandra, sql, or timescale (for hybrid mode, DATABASE_TS_TYPE value should be cassandra, or timescale)
```

Execute regular upgrade script:

```bash
sudo /usr/share/sentient/bin/install/upgrade.sh --fromVersion=3.2.1
```
{: .copy-code}

#### Start the service

{% include templates/redis-post-upgrade-notice.md %}

```bash
sudo service sentient start
```
{: .copy-code}

### Upgrading SENTIENT Professional Edition to 3.2.1

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.2PE. In order to upgrade to 3.2.1PE you need to [**upgrade to 3.2PE first**](#upgrading-sentient-pe-to-32).
[**Prepare**](#prepare-for-upgrading-sentient) for upgrading SENTIENT.

#### SENTIENT Professional Edition package download

{% include resources.liquid version="3.2.1" kind="ubuntu-download" pe="true" %}  

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT service if it is running.

```bash
sudo service sentient stop
```
{: .copy-code}

* Install Sentient Web Report component as described [here](/docs/user-guide/install/pe/ubuntu/#step-9-install-sentient-webreport-component).


{% include resources.liquid version="3.2.1" kind="ubuntu-installation" pe="true" %}  

**NOTE:** Package installer will ask you to merge your sentient configuration. It is preferred to use **merge option** to make sure that all your previous parameters will not be overwritten.  
Please make sure that you set **database.ts.type** parameter value (in the file **/etc/sentient/conf/sentient.yml**) to "cassandra" instead of "sql" if you are using Cassandra database for timeseries data:

```
database:
  ts_max_intervals: "${DATABASE_TS_MAX_INTERVALS:700}" # Max number of DB queries generated by single API call to fetch telemetry records
  ts:
    type: "${DATABASE_TS_TYPE:sql}" # cassandra, sql, or timescale (for hybrid mode, DATABASE_TS_TYPE value should be cassandra, or timescale)
```

Execute regular upgrade script:

```bash
sudo /usr/share/sentient/bin/install/upgrade.sh --fromVersion=3.2.0
```
{: .copy-code}

#### Start the service

{% include templates/redis-post-upgrade-notice.md %}

```bash
sudo service sentient start
```
{: .copy-code}

### Upgrading SENTIENT Professional Edition to 3.2

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.1.1PE. In order to upgrade to 3.2PE you need to [**upgrade to 3.1.1PE first**](/docs/pe/user-guide/install/upgrade-instructions/ubuntu/v3-1-x/#upgrading-sentient-pe-to-311).
[**Prepare**](#prepare-for-upgrading-sentient) for upgrading SENTIENT.

#### SENTIENT Professional Edition package download

{% include resources.liquid version="3.2" kind="ubuntu-download" pe="true" %}  

#### SENTIENT Professional Edition service upgrade

* Stop SENTIENT service if it is running.

```bash
sudo service sentient stop
```
{: .copy-code}

* Install Sentient Web Report component as described [here](/docs/user-guide/install/pe/ubuntu/#step-9-install-sentient-webreport-component).


{% include resources.liquid version="3.2" kind="ubuntu-installation" pe="true" %}  

**NOTE:** Package installer will ask you to merge your sentient configuration. It is preferred to use **merge option** to make sure that all your previous parameters will not be overwritten.  
Please make sure that you set **database.ts.type** parameter value (in the file **/etc/sentient/conf/sentient.yml**) to "cassandra" instead of "sql" if you are using Cassandra database for timeseries data:

```
database:
  ts_max_intervals: "${DATABASE_TS_MAX_INTERVALS:700}" # Max number of DB queries generated by single API call to fetch telemetry records
  ts:
    type: "${DATABASE_TS_TYPE:sql}" # cassandra, sql, or timescale (for hybrid mode, DATABASE_TS_TYPE value should be cassandra, or timescale)
```

Execute regular upgrade script:

```bash
sudo /usr/share/sentient/bin/install/upgrade.sh --fromVersion=3.1.1
```
{: .copy-code}

#### Start the service

{% include templates/redis-post-upgrade-notice.md %}

```bash
sudo service sentient start
```
{: .copy-code}

