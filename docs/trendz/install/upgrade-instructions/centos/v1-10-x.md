---
layout: docwithnav-sentient-analytics
title: SENTIENT ANALYTICS v1.10.x upgrade instructions for CentOS
description: SENTIENT ANALYTICS v1.10.x upgrade guide for CentOS
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/sentient-analytics/install/upgrade-instructions/'
---

* TOC
{:toc}

{% include docs/sentient-analytics/install/prepare.md %}

### Upgrading SENTIENT ANALYTICS to 1.10.3-HF7

{% capture difference %}
**NOTE:**
<br>
[**Prepare**](#prepare-for-upgrading-sentient-analytics-analytics) for upgrading SENTIENT ANALYTICS.
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT ANALYTICS package download

{% include resources.liquid version="1.10.3-HF7" kind="centos-download" sentient-analytics="true" %}

#### SENTIENT ANALYTICS service upgrade

* Install SENTIENT ANALYTICS service

{% include resources.liquid version="1.10.3-HF7" kind="centos-installation" sentient-analytics="true" %}

**NOTE:** Package installer will ask you to merge your SENTIENT ANALYTICS configuration. It is preferred to use **merge option** to make sure that all your previous parameters will not be overwritten.

Execute regular upgrade script:

```bash
sudo /usr/share/sentient-analytics/bin/install/upgrade.sh --fromVersion=1.10.3
```
{: .copy-code}

#### Start the service

```bash
sudo service sentient-analytics start
```
{: .copy-code}

### Upgrading SENTIENT ANALYTICS to 1.10.3

{% capture difference %}
**NOTE:**
<br>
[**Prepare**](#prepare-for-upgrading-sentient-analytics-analytics) for upgrading SENTIENT ANALYTICS.
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT ANALYTICS package download

{% include resources.liquid version="1.10.3" kind="centos-download" sentient-analytics="true" %}

#### SENTIENT ANALYTICS service upgrade

* Install SENTIENT ANALYTICS service

{% include resources.liquid version="1.10.3" kind="centos-installation" sentient-analytics="true" %}

**NOTE:** Package installer will ask you to merge your SENTIENT ANALYTICS configuration. It is preferred to use **merge option** to make sure that all your previous parameters will not be overwritten.

Execute regular upgrade script:

```bash
sudo /usr/share/sentient-analytics/bin/install/upgrade.sh --fromVersion=1.10.2
```
{: .copy-code}

#### Start the service

```bash
sudo service sentient-analytics start
```
{: .copy-code}

### Upgrading SENTIENT ANALYTICS to 1.10.2

{% capture difference %}
**NOTE:**
<br>
[**Prepare**](#prepare-for-upgrading-sentient-analytics-analytics) for upgrading SENTIENT ANALYTICS.
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT ANALYTICS package download

{% include resources.liquid version="1.10.2" kind="centos-download" sentient-analytics="true" %}

#### SENTIENT ANALYTICS service upgrade

* Install SENTIENT ANALYTICS service

{% include resources.liquid version="1.10.2" kind="centos-installation" sentient-analytics="true" %}

**NOTE:** Package installer will ask you to merge your SENTIENT ANALYTICS configuration. It is preferred to use **merge option** to make sure that all your previous parameters will not be overwritten.

Execute regular upgrade script:

```bash
sudo /usr/share/sentient-analytics/bin/install/upgrade.sh --fromVersion=1.10.1
```
{: .copy-code}

#### Start the service

```bash
sudo service sentient-analytics start
```
{: .copy-code}

### Upgrading SENTIENT ANALYTICS to 1.10.1

{% capture difference %}
**NOTE:**
<br>
[**Prepare**](#prepare-for-upgrading-sentient-analytics-analytics) for upgrading SENTIENT ANALYTICS.
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT ANALYTICS package download

{% include resources.liquid version="1.10.1" kind="centos-download" sentient-analytics="true" %}

#### SENTIENT ANALYTICS service upgrade

* Install SENTIENT ANALYTICS service

{% include resources.liquid version="1.10.1" kind="centos-installation" sentient-analytics="true" %}

**NOTE:** Package installer will ask you to merge your SENTIENT ANALYTICS configuration. It is preferred to use **merge option** to make sure that all your previous parameters will not be overwritten.

Execute regular upgrade script:

```bash
sudo /usr/share/sentient-analytics/bin/install/upgrade.sh --fromVersion=1.10.0
```
{: .copy-code}

#### Start the service

```bash
sudo service sentient-analytics start
```
{: .copy-code}

### Upgrading SENTIENT ANALYTICS to 1.10.0

{% capture difference %}
**NOTE:**
<br>
[**Prepare**](#prepare-for-upgrading-sentient-analytics-analytics) for upgrading SENTIENT ANALYTICS.
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT ANALYTICS package download

{% include resources.liquid version="1.10.0" kind="centos-download" sentient-analytics="true" %}

#### SENTIENT ANALYTICS service upgrade

* Stop SENTIENT ANALYTICS service if it is running.

```bash
sudo service sentient-analytics stop
```
{: .copy-code}

* Install latest SENTIENT ANALYTICS service

{% include resources.liquid version="1.10.0" kind="centos-installation" sentient-analytics="true" %}

**NOTE:** Package installer will ask you to merge your SENTIENT ANALYTICS configuration. It is preferred to use **merge option** to make sure that all your previous parameters will not be overwritten.

Execute regular upgrade script:

```bash
sudo /usr/share/sentient-analytics/bin/install/upgrade.sh --fromVersion=1.9.2
```
{: .copy-code}

#### Start the service

```bash
sudo service sentient-analytics start
```
{: .copy-code}
