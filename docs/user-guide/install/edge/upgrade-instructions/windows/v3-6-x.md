---
layout: docwithnav-edge
title: SENTIENT Edge CE v3.6.x upgrade instructions for Windows
description: SENTIENT Edge CE v3.6.x upgrade guide for Windows
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/edge/upgrade-instructions/'
---

* TOC
{:toc}

{% include templates/edge/install/windows-backup-before-upgrade.md %}

## Upgrading Edge CE to 3.6.4

{% assign updateServerLink = "windows/v3-6-x/#upgrading-sentient-ce-to-364" %}
{% assign previousVersion = "3.6.3" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download SENTIENT Edge package for Windows: [tb-edge-windows-3.6.4 zip](https://github.com/sentient/sentient-gateway/releases/download/v3.6.4/tb-edge-windows-3.6.4.zip).

Stop the Edge service:

```bash
net stop tb-edge
```
{: .copy-code}

Make a backup of your SENTIENT Edge configuration located in **\<SENTIENT Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

Extract and update configuration:
* Extract the SENTIENT Edge package.
* Compare your old configuration files (from the backup) with the new ones.
* Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```bash
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the service

```bash
net start tb-edge
```
{: .copy-code}

## Upgrading Edge CE to 3.6.3

{% assign updateServerLink = "windows/v3-6-x/#upgrading-sentient-ce-to-363" %}
{% assign previousVersion = "3.6.2" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download SENTIENT Edge package for Windows: [tb-edge-windows-3.6.3 zip](https://github.com/sentient/sentient-gateway/releases/download/v3.6.3/tb-edge-windows-3.6.3.zip).

### Stop the Edge service

```bash
net stop tb-edge
```
{: .copy-code}

### Back up the configuration

Make a backup of your SENTIENT Edge configuration located in **\<SENTIENT Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

### Extract and update configuration

1. Extract the SENTIENT Edge package.
2. Compare your old configuration files (from the backup) with the new ones.
3. Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```bash
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the service

```text
net start tb-edge
```
{: .copy-code}


## Upgrading Edge CE to 3.6.2

{% assign updateServerLink = "windows/v3-6-x/#upgrading-sentient-ce-to-362" %}
{% assign previousVersion = "3.6.1" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download SENTIENT Edge package for Windows: [tb-edge-windows-3.6.2 zip](https://github.com/sentient/sentient-gateway/releases/download/v3.6.2/tb-edge-windows-3.6.2.zip).

### SENTIENT Edge service upgrade

* Stop SENTIENT Edge service if it is running:

```text
net stop tb-edge
```
{: .copy-code}

### Back up the configuration

Make a backup of your SENTIENT Edge configuration located in **\<SENTIENT Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

### Extract and update configuration

1. Extract the SENTIENT Edge package.
2. Compare your old configuration files (from the backup) with the new ones.
3. Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```text
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the service

```text
net start tb-edge
```
{: .copy-code}

## Upgrading Edge CE to 3.6.1

{% assign updateServerLink = "windows/v3-6-x/#upgrading-sentient-ce-to-361" %}
{% assign previousVersion = "3.6.0" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download SENTIENT Edge package for Windows: [tb-edge-windows-3.6.1 zip](https://github.com/sentient/sentient-gateway/releases/download/v3.6.1/tb-edge-windows-3.6.1.zip).

### Stop the Edge service

```text
net stop tb-edge
```
{: .copy-code}

### Back up the configuration

Make a backup of your SENTIENT Edge configuration located in **\<SENTIENT Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

### Extract and update configuration

1. Extract the SENTIENT Edge package.
2. Compare your old configuration files (from the backup) with the new ones.
3. Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```text
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the service

```text
net start tb-edge
```
{: .copy-code}

## Upgrading Edge CE to 3.6.0

{% assign updateServerLink = "windows/v3-6-x/#upgrading-sentient-ce-to-36" %}
{% assign previousVersion = "3.5.1.1" %}
{% include templates/edge/install/compatibility-warning-version.md %}

### Download the Edge package

Download SENTIENT Edge package for Windows: [tb-edge-windows-3.6 zip](https://github.com/sentient/sentient-gateway/releases/download/v3.6/tb-edge-windows-3.6.zip).

### Stop the Edge service

```text
net stop tb-edge
```
{: .copy-code}

### Back up the configuration

Make a backup of your SENTIENT Edge configuration located in **\<SENTIENT Edge install dir\>\conf** (for example, **C:\tb-edge\conf**).

### Extract and update configuration

1. Extract the SENTIENT Edge package.
2. Compare your old configuration files (from the backup) with the new ones.
3. Merge any custom settings into the new configuration files.

### Run the upgrade script

**Note:** The upgrade script must be run with Administrator privileges.

```text
C:\tb-edge>upgrade.bat --fromVersion={{previousVersion}}
```
{: .copy-code}

### Start the service

```text
net start tb-edge
```
{: .copy-code}
