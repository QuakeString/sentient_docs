---
layout: docwithnav
title: SENTIENT v3.3.x upgrade instructions for Docker Compose
description: SENTIENT v3.3.x upgrade guide for Docker Compose
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/upgrade-instructions/'
---

* TOC
{:toc}

### Upgrading SENTIENT to 3.3.4.1

{% capture difference %}
**NOTE:**
<br>
These upgrade steps are applicable for SENTIENT version 3.3.4. In order to upgrade to 3.3.4.1 you need to [**upgrade to 3.3.4 first**](#upgrading-sentient-ce-to-334).
{% endcapture %}
{% include templates/info-banner.md content=difference %}

{% capture update_manifests_note %}
We strongly recommend that you also update your Docker Compose deployment manifests to [**the latest available version from GitHub repository**](https://github.com/sentient/sentient/tree/master/docker) with every SENTIENT upgrade. This ensures your SENTIENT service runs with the latest best-practice parameters and configurations, while also guarantees that all required services are correctly deployed to support the newest features introduced in the release.
<br>
<br>
If you are running older releases of SENTIENT - be aware that the manifests in `master` branch of the repository may contain configurations for features that are not available in your specific version. Always ensure that manifests are compatible with your target SENTIENT version while merging deployment files.
{% endcapture %}
{% include templates/warn-banner.md content=update_manifests_note %}

#### SENTIENT service upgrade

1. Change the parameter `TB_VERSION` in the `.env` file.

  ```.env
  TB_VERSION=3.3.4.1
  ```

2. Execute the following commands:

  ```bash
  ./docker-stop-services.sh
  ./docker-start-services.sh
  ```
  {: .copy-code}

### Upgrading SENTIENT to 3.3.4

{% capture difference %}
**NOTE:**
<br>
These upgrade steps are applicable for SENTIENT version 3.3.3. In order to upgrade to 3.3.4 you need to [**upgrade to 3.3.3 first**](#upgrading-sentient-ce-to-333).
{% endcapture %}
{% include templates/info-banner.md content=difference %}

{% capture update_manifests_note %}
We strongly recommend that you also update your Docker Compose deployment manifests to [**the latest available version from GitHub repository**](https://github.com/sentient/sentient/tree/master/docker) with every SENTIENT upgrade. This ensures your SENTIENT service runs with the latest best-practice parameters and configurations, while also guarantees that all required services are correctly deployed to support the newest features introduced in the release.
<br>
<br>
If you are running older releases of SENTIENT - be aware that the manifests in `master` branch of the repository may contain configurations for features that are not available in your specific version. Always ensure that manifests are compatible with your target SENTIENT version while merging deployment files.
{% endcapture %}
{% include templates/warn-banner.md content=update_manifests_note %}

#### SENTIENT service upgrade

{% include docs/user-guide/install/docker-compose-upgrade-steps-fromversion.md version="3.3.4" fromversion="3.3.3" %}

### Upgrading SENTIENT to 3.3.3

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.2. In order to upgrade to 3.3.3 you need to [**upgrade to 3.3.2 first**](#upgrading-sentient-ce-to-332).

{% include templates/install/tb-333-update.md %}

{% capture update_manifests_note %}
We strongly recommend that you also update your Docker Compose deployment manifests to [**the latest available version from GitHub repository**](https://github.com/sentient/sentient/tree/master/docker) with every SENTIENT upgrade. This ensures your SENTIENT service runs with the latest best-practice parameters and configurations, while also guarantees that all required services are correctly deployed to support the newest features introduced in the release.
<br>
<br>
If you are running older releases of SENTIENT - be aware that the manifests in `master` branch of the repository may contain configurations for features that are not available in your specific version. Always ensure that manifests are compatible with your target SENTIENT version while merging deployment files.
{% endcapture %}
{% include templates/warn-banner.md content=update_manifests_note %}

#### SENTIENT service upgrade

{% include docs/user-guide/install/docker-compose-upgrade-steps-fromversion.md version="3.3.3" fromversion="3.3.2" %}

### Upgrading SENTIENT to 3.3.2

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.1. In order to upgrade to 3.3.2 you need to [**upgrade to 3.3.1 first**](#upgrading-sentient-ce-to-331).

{% capture update_manifests_note %}
We strongly recommend that you also update your Docker Compose deployment manifests to [**the latest available version from GitHub repository**](https://github.com/sentient/sentient/tree/master/docker) with every SENTIENT upgrade. This ensures your SENTIENT service runs with the latest best-practice parameters and configurations, while also guarantees that all required services are correctly deployed to support the newest features introduced in the release.
<br>
<br>
If you are running older releases of SENTIENT - be aware that the manifests in `master` branch of the repository may contain configurations for features that are not available in your specific version. Always ensure that manifests are compatible with your target SENTIENT version while merging deployment files.
{% endcapture %}
{% include templates/warn-banner.md content=update_manifests_note %}

#### SENTIENT service upgrade

{% include docs/user-guide/install/docker-compose-upgrade-steps-fromversion.md version="3.3.2" fromversion="3.3.1" %}

### Upgrading SENTIENT to 3.3.1

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.0. In order to upgrade to 3.3.1 you need to [**upgrade to 3.3.0 first**](#upgrading-sentient-ce-to-33).

{% capture update_manifests_note %}
We strongly recommend that you also update your Docker Compose deployment manifests to [**the latest available version from GitHub repository**](https://github.com/sentient/sentient/tree/master/docker) with every SENTIENT upgrade. This ensures your SENTIENT service runs with the latest best-practice parameters and configurations, while also guarantees that all required services are correctly deployed to support the newest features introduced in the release.
<br>
<br>
If you are running older releases of SENTIENT - be aware that the manifests in `master` branch of the repository may contain configurations for features that are not available in your specific version. Always ensure that manifests are compatible with your target SENTIENT version while merging deployment files.
{% endcapture %}
{% include templates/warn-banner.md content=update_manifests_note %}

#### SENTIENT service upgrade

{% include docs/user-guide/install/docker-compose-upgrade-steps-fromversion.md version="3.3.1" fromversion="3.3.0" %}

### Upgrading SENTIENT to 3.3

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.2.2. In order to upgrade to 3.3 you need to [**upgrade to 3.2.2 first**](/docs/user-guide/install/upgrade-instructions/docker-compose/v3-2-x/#upgrading-sentient-ce-to-322).

{% capture update_manifests_note %}
We strongly recommend that you also update your Docker Compose deployment manifests to [**the latest available version from GitHub repository**](https://github.com/sentient/sentient/tree/master/docker) with every SENTIENT upgrade. This ensures your SENTIENT service runs with the latest best-practice parameters and configurations, while also guarantees that all required services are correctly deployed to support the newest features introduced in the release.
<br>
<br>
If you are running older releases of SENTIENT - be aware that the manifests in `master` branch of the repository may contain configurations for features that are not available in your specific version. Always ensure that manifests are compatible with your target SENTIENT version while merging deployment files.
{% endcapture %}
{% include templates/warn-banner.md content=update_manifests_note %}

#### SENTIENT service upgrade

{% include docs/user-guide/install/docker-compose-upgrade-steps-fromversion.md version="3.3.0" fromversion="3.2.2" %}