---
layout: docwithnav
title: SENTIENT v3.1.x upgrade instructions for Docker
description: SENTIENT v3.1.x upgrade guide for Docker
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/upgrade-instructions/'
---

* TOC
{:toc}

### Upgrading SENTIENT to 3.1.1

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.1. In order to upgrade to 3.1.1 you need to [**upgrade to 3.1CE first**](#upgrading-sentient-ce-to-31).

#### SENTIENT image download

Download SENTIENT Docker image:

```bash
docker pull sentient/tb-postgres:3.1.1
```
{: .copy-code}

#### SENTIENT service upgrade

* Stop SENTIENT `mytb` container if it is running.

```bash
docker compose stop mytb
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytb**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbce:
    image: "sentient/tb-postgres:3.1.1"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.1.0' | sudo tee ~/.mytb-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker run -it -v ~/.mytb-data:/data --rm sentient/tb-postgres:3.1.1 upgrade-tb.sh
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT to 3.1

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.0.1. In order to upgrade to 3.1 you need to [**upgrade to 3.0.1 first**](/docs/user-guide/install/upgrade-instructions/docker/v3-0-x/#upgrading-sentient-ce-to-301).

#### SENTIENT image download

Download SENTIENT Docker image:

```bash
docker pull sentient/tb-postgres:3.1.0
```
{: .copy-code}

#### SENTIENT service upgrade

* Stop SENTIENT `mytb` container if it is running.

```bash
docker compose stop mytb
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytb**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbce:
    image: "sentient/tb-postgres:3.1.0"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.0.1' | sudo tee ~/.mytb-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker run -it -v ~/.mytb-data:/data --rm sentient/tb-postgres:3.1.0 upgrade-tb.sh
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}