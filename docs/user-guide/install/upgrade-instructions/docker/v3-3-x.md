---
layout: docwithnav
title: SENTIENT v3.3.x upgrade instructions for Docker
description: SENTIENT v3.3.x upgrade guide for Docker
active-menu-item-click: "true"
breadcrumbs: true
breadcrumbs-steps: 2
breadcrumbs-show: 2
effective-url: '/docs/user-guide/install/upgrade-instructions/'
---

* TOC
{:toc}

### Upgrading SENTIENT to 3.3.4.1

{% capture difference %}
**NOTE:**
<br>
These upgrade steps are applicable for SENTIENT version 3.3.4. In order to upgrade to 3.3.4.1 you need to [**upgrade to 3.3.4 first**](#upgrading-sentient-ce-to-334).
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT image download

Download SENTIENT Docker image:

```bash
docker pull sentient/tb-postgres:3.3.4.1
```
{: .copy-code}

#### SENTIENT service upgrade

* Stop SENTIENT `mytb` container if it is running.

```bash
docker compose stop mytb
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytb**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbce:
    image: "sentient/tb-postgres:3.3.4.1"
< . . . >
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT to 3.3.4

{% capture difference %}
**NOTE:**
<br>
These upgrade steps are applicable for SENTIENT version 3.3.3. In order to upgrade to 3.3.4 you need to [**upgrade to 3.3.3 first**](#upgrading-sentient-ce-to-333).
{% endcapture %}
{% include templates/info-banner.md content=difference %}

#### SENTIENT image download

Download SENTIENT Docker image:

```bash
docker pull sentient/tb-postgres:3.3.4
```
{: .copy-code}

#### SENTIENT service upgrade

* Stop SENTIENT `mytb` container if it is running.

```bash
docker compose stop mytb
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytb**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbce:
    image: "sentient/tb-postgres:3.3.4"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.3.3' | sudo tee ~/.mytb-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker run -it -v ~/.mytb-data:/data --rm sentient/tb-postgres:3.3.4 upgrade-tb.sh
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT to 3.3.3

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.2. In order to upgrade to 3.3.3 you need to [**upgrade to 3.3.2 first**](#upgrading-sentient-ce-to-332).

{% include templates/install/tb-333-update.md %}

#### SENTIENT image download

Download SENTIENT Docker image:

```bash
docker pull sentient/tb-postgres:3.3.3
```
{: .copy-code}

#### SENTIENT service upgrade

* Stop SENTIENT `mytb` container if it is running.

```bash
docker compose stop mytb
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytb**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbce:
    image: "sentient/tb-postgres:3.3.3"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.3.2' | sudo tee ~/.mytb-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker run -it -v ~/.mytb-data:/data --rm sentient/tb-postgres:3.3.3 upgrade-tb.sh
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT to 3.3.2

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.1. In order to upgrade to 3.3.2 you need to [**upgrade to 3.3.1 first**](#upgrading-sentient-ce-to-331).

#### SENTIENT image download

Download SENTIENT Docker image:

```bash
docker pull sentient/tb-postgres:3.3.2
```
{: .copy-code}

#### SENTIENT service upgrade

* Stop SENTIENT `mytb` container if it is running.

```bash
docker compose stop mytb
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytb**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbce:
    image: "sentient/tb-postgres:3.3.2"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.3.1' | sudo tee ~/.mytb-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker run -it -v ~/.mytb-data:/data --rm sentient/tb-postgres:3.3.2 upgrade-tb.sh
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT to 3.3.1

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.3.0. In order to upgrade to 3.3.1 you need to [**upgrade to 3.3.0 first**](#upgrading-sentient-ce-to-33).

#### SENTIENT image download

Download SENTIENT Docker image:

```bash
docker pull sentient/tb-postgres:3.3.1
```
{: .copy-code}

#### SENTIENT service upgrade

* Stop SENTIENT `mytb` container if it is running.

```bash
docker compose stop mytb
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytb**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbce:
    image: "sentient/tb-postgres:3.3.1"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.3.0' | sudo tee ~/.mytb-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker run -it -v ~/.mytb-data:/data --rm sentient/tb-postgres:3.3.1 upgrade-tb.sh
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

### Upgrading SENTIENT to 3.3

**NOTE**: These upgrade steps are applicable for SENTIENT version 3.2.2. In order to upgrade to 3.3 you need to [**upgrade to 3.2.2 first**](/docs/user-guide/install/upgrade-instructions/docker/v3-2-x/#upgrading-sentient-ce-to-322).

#### SENTIENT image download

Download SENTIENT Docker image:

```bash
docker pull sentient/tb-postgres:3.3.0
```
{: .copy-code}

#### SENTIENT service upgrade

* Stop SENTIENT `mytb` container if it is running.

```bash
docker compose stop mytb
```
 {: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose stop mytb**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}

* Update docker-compose.yml

```yml
< . . . >
  mytbce:
    image: "sentient/tb-postgres:3.3.0"
< . . . >
```
{: .copy-code}

* Change `upgradeversion` variable:

```bash
echo '3.2.2' | sudo tee ~/.mytb-data/.upgradeversion
```
{: .copy-code}

* Execute the following commands to upgrade SENTIENT to the new version:

```bash
docker run -it -v ~/.mytb-data:/data --rm sentient/tb-postgres:3.3.0 upgrade-tb.sh
```
{: .copy-code}

* Start SENTIENT:

```bash
docker compose up -d
```
{: .copy-code}

{% capture dockerComposeStandalone %}
If you still rely on Docker Compose as docker-compose (with a hyphen) execute next command:
<br>**docker-compose up -d**
{% endcapture %}
{% include templates/info-banner.md content=dockerComposeStandalone %}